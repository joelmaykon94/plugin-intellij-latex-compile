<idea-plugin>
    <id>com.github.joelmaykon94.intellij-latex-plugin</id>
    <name>LaTeX Compile &amp; Preview</name>
    <version>2026.1.1.4</version>
    <vendor email="joelmaykon94@gmail.com" url="https://github.com/joelmaykon94/plugin-intellij-latex-compile">Joel Maykon</vendor>

    <description><![CDATA[
        <h2>High-performance real-time LaTeX compilation and PDF preview for IntelliJ Platform IDEs.</h2>

        <p><b>LaTeX Compile &amp; Preview</b> transforms your IntelliJ IDE into a responsive, distraction-free LaTeX authoring environment with side-by-side code editing, reactive background auto-compilation, and embedded PDF rendering.</p>

        <h3>✨ Key Features</h3>
        <ul>
            <li><b>Native Split View:</b> Work seamlessly with LaTeX source code on the left and a live PDF preview on the right.</li>
            <li><b>Code Completion &amp; Snippets:</b> Intelligent autocomplete for LaTeX commands (<code>\section</code>, <code>\cite</code>, <code>\ref</code>, <code>\textbf</code>, etc.), environment templates (<code>\begin{figure}</code>, <code>\begin{table}</code>, <code>\begin{equation}</code>, <code>\begin{itemize}</code>), popular packages, and document classes with automatic closing braces and cursor positioning.</li>
            <li><b>Line Commenting (Ctrl+/):</b> Native line commenting and uncommenting using <code>%</code> prefix.</li>
            <li><b>Reactive Auto-Compile:</b> Fast, debounced (600ms) background compilation on typing and saving with per-file mutex locking to prevent concurrent <code>latexmk</code> collisions.</li>
            <li><b>Embedded Chromium Preview (JCEF + PDF.js):</b> Modern PDF rendering with strict sequential page loading, eliminating race conditions that previously reordered bibliography or appendix pages.</li>
            <li><b>Scroll Synchronization:</b> Proportional scrolling between the LaTeX editor and PDF preview, with an instant toolbar toggle (<code>Sync: ON / OFF</code>).</li>
            <li><b>Advanced Zoom &amp; Navigation:</b> Step zoom (25% to 400%), quick 100%/150% toggle, Fit-to-Width mode, mouse-wheel zoom (<code>Ctrl/Cmd + Scroll</code>), and keyboard shortcuts.</li>
            <li><b>One-Click PDF Export:</b> Save the compiled PDF to any local directory directly from the preview toolbar using the native file chooser.</li>
            <li><b>Reverse Navigation (SyncTeX):</b> Double-click any rendered page in the PDF preview to jump directly to the corresponding source line in the editor.</li>
            <li><b>Syntax Highlighting:</b> Fast, deterministic lexer providing syntax coloring for LaTeX commands, environments, inline/display math (<code>$...$</code>, <code>$$...$$</code>), comments, delimiters, and escaped characters.</li>
            <li><b>Compiler Error Reporting:</b> Clear error diagnostics extracted directly from <code>latexmk</code> logs displayed in an in-editor overlay panel.</li>
        </ul>

        <h3>📋 Requirements &amp; Additional Information</h3>
        <ul>
            <li><b>LaTeX Distribution:</b> Requires a local TeX distribution (TeX Live, MacTeX, or MiKTeX) with <code>latexmk</code> available in the system <code>PATH</code>.</li>
            <li><b>Runtime:</b> Requires JetBrains Runtime (JBR) with JCEF enabled (standard in JetBrains IDEs).</li>
            <li><b>Supported File Types:</b> <code>.tex</code>, <code>.sty</code>, <code>.cls</code>, <code>.dtx</code>, <code>.ins</code>.</li>
            <li><b>Supported IDEs:</b> IntelliJ IDEA (Ultimate &amp; Community), PyCharm, WebStorm, CLion, GoLand, Rider, Android Studio (2024.2 to 2026.x).</li>
            <li><b>🔒 Security &amp; Privacy:</b> 100% local processing. No telemetry, no external network requests, and no document contents sent to third-party services.</li>
        </ul>
    ]]></description>

    <change-notes><![CDATA[
        <h3>Version 2026.1.1.4</h3>
        <ul>
            <li><b>💡 Code Completion:</b> Context-aware autocompletion for standard LaTeX commands, environments, popular packages, and document classes.</li>
            <li><b>📐 Smart Environment Templates:</b> Automated snippet expansion for <code>figure</code>, <code>table</code>, <code>equation</code>, <code>align</code>, <code>itemize</code>, and <code>enumerate</code> with automatic closing tags and cursor placement.</li>
            <li><b>💬 Line Commenting:</b> Native line comment support (<code>Ctrl + /</code> or <code>Cmd + /</code>) using <code>%</code> delimiters.</li>
            <li><b>⚡ Zero-Overhead PSI &amp; Parser Definition:</b> Integrated flat AST parser definition enabling IntelliJ code insight features without risk of false-positive syntax errors.</li>
            <li><b>🧪 Automated Test Coverage:</b> Added unit test suite covering completion dictionaries, environment templates, commenter, and parser definitions.</li>
        </ul>
    ]]></change-notes>

    <depends>com.intellij.modules.platform</depends>
    <depends optional="true">com.intellij.modules.jcef</depends>

    <extensions defaultExtensionNs="com.intellij">
        <!-- File Type -->
        <fileType
            name="LaTeX"
            implementationClass="com.github.joelmaykon94.latex.lang.LatexFileType"
            fieldName="INSTANCE"
            language="LaTeX"
            extensions="tex;sty;cls;dtx;ins"/>

        <!-- Parser Definition (Lightweight PSI) -->
        <lang.parserDefinition
            language="LaTeX"
            implementationClass="com.github.joelmaykon94.latex.psi.LatexParserDefinition"/>

        <!-- Syntax Highlighting -->
        <lang.syntaxHighlighterFactory
            language="LaTeX"
            implementationClass="com.github.joelmaykon94.latex.highlighting.LatexSyntaxHighlighterFactory"/>

        <!-- Line Commenter (Ctrl+/) -->
        <lang.commenter
            language="LaTeX"
            implementationClass="com.github.joelmaykon94.latex.commenter.LatexCommenter"/>

        <!-- Code Completion -->
        <completion.contributor
            language="LaTeX"
            implementationClass="com.github.joelmaykon94.latex.completion.LatexCompletionContributor"/>

        <!-- Split Editor (Source + PDF Preview) -->
        <fileEditorProvider
            implementation="com.github.joelmaykon94.latex.editor.LatexSplitEditorProvider"/>

        <!-- Auto-Compile Service (Project-level) -->
        <projectService
            serviceImplementation="com.github.joelmaykon94.latex.compiler.LatexAutoCompileService"/>

        <!-- Project Startup Activity to initialize auto-compile -->
        <postStartupActivity
            implementation="com.github.joelmaykon94.latex.compiler.LatexStartupActivity"/>
    </extensions>
</idea-plugin>
